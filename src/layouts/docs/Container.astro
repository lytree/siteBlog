---
interface Props {
    defaultPadding: boolean
}

const { defaultPadding } = Astro.props;
---

<div class="flex -mt-32 md:px-4 xl:px-0 xl:gap-4">
    <main class={`w-full max-w-4xl z-10 ${defaultPadding ? "p-5 md:p-6" : ""}`}>
        <slot></slot>
    </main>
    <div class="w-full max-w-4xl h-64 bg-white bg-opacity-70 dark:bg-gray-950 dark:bg-opacity-70 md:rounded-3xl backdrop-blur-xl" aria-hidden="true"></div>
</div>
